// Header3 is the edit-card view, accessed via /credit-card-validation/editCard route.
// The implementation lives in editCard.js — re-export it here to satisfy the App.js import.
export { default } from './editCard';
